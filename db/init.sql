-- Crea las dos bases que usa el stack al iniciar Postgres por primera vez.
-- publilat  -> la app (DATABASE_URL del backend)
-- evolution -> Evolution API (WhatsApp)
CREATE DATABASE publilat;
CREATE DATABASE evolution;
